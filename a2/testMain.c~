#include <stdio.h>
#include <string.h>
#include "defs.h"

#define MAX_STR      32
#define MAX_ARR_SIZE 32

int main(int argc, char *argv[]){

  int i;
  char str1[MAX_STR];
  char str2[MAX_STR];
  /*
  for(i=0; i<argc; i++) {
    printf("Argument %d is %s\n", i, argv[i]);
  }
  */

  while(1){
    printf("Please enter your TASSLA program: \n");
    scanf("%s", str);
    printf("%s \n", str);
    
    if(strcmp(str1, "END" == 0)
       break;




  }
  return 0;
}
